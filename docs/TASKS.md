# Tasks

## Active

1. Maintain source-of-truth boundaries between root docs, `nakarin_erp`, and service runtime knowledge.
2. Keep skill registry metadata validation strict and reviewed.
3. Complete legacy skill consumer audits before removing legacy entries.

## Backlog

1. Remove validator allowlist exceptions after skill refactors pass validation.
2. Add CI check for `scripts/validate_skill_registry.py`.
