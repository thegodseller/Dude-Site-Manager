# Android TV Integration

- Used by `room_service`.
- Likely surfaces room content, service ordering, and guest interaction.
- Device fleet model, command set, and failure handling are `UNKNOWN: requires owner confirmation`.
